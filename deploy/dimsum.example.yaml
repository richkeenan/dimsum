# Native service defaults. Configuration is preserved during upgrades.
version: 1
dns:
  listen: ["0.0.0.0:53"]
  upstreams: ["1.1.1.1:53", "9.9.9.9:53"]
admin:
  listen: "0.0.0.0:8080"
  control_socket: /run/dimsum/control.sock
paths:
  data_dir: /var/lib/dimsum/data
  secrets_dir: /etc/dimsum/secrets
